{
  "name": "mytasks-client",
  "version": "0.4.0",
  "description": "Client component for a simple task manager web application",
  "dependencies": {
    "gulp": "^3.8.11",
    "gulp-util": "^3.0.4",
    "bower": "^1.7.9"
  },
  "scripts": {
    "build": "gulp build",
    "bower-install": "bower install"
  },
  "engines": {
    "node": ">=0.10.0"
  }
}
